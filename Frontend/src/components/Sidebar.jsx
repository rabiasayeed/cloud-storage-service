const items = [['My files', '[]'], ['Recent', 'R'], ['Starred', '*'], ['Shared with me', 'S'], ['Trash', 'T']]
export default function Sidebar({ active, onNavigate, onUpload, user, onLogout }) {
  return <aside className="sidebar"><div className="brand"><b>*</b> Cloudly</div><button className="upload" onClick={onUpload}>+ Upload files</button><nav>{items.map(([name, icon]) => <button key={name} className={active === name ? 'active' : ''} onClick={() => onNavigate?.(name)}><span className="icon">{icon}</span>{name}</button>)}</nav><div className="side-bottom"><div className="storage"><span>Storage</span><span>7.2 GB / 15 GB</span></div><div className="progress"><i /></div><button className="upgrade">* <span><strong>Get more storage</strong><small>Upgrade your plan</small></span>&gt;</button><button onClick={onLogout}>Sign out</button><div className="profile"><b>{(user?.name || 'JD').slice(0, 2).toUpperCase()}</b><span><strong>{user?.name || 'Jordan Davis'}</strong><small>Free plan</small></span></div></div></aside>
}